[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "quantrade-research"
version = "0.1.0"
description = "Quantitative research service for Quantrade"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/quantrade_research"]
