# Synthetic local demonstration only. Copy this file to .env.demo and replace
# the local PostgreSQL password. No market-data or SEC credentials are needed.
QUANTRADE_ENVIRONMENT=test
SCORE_ELIGIBILITY_CONTRACT=exact_zero_coefficients_v1
DATABASE_URL=postgresql://postgres:replace-with-local-password@localhost:5432/quantrade_demo
